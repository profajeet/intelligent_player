services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - backend_storage:/app/storage
    env_file:
      - ./backend/.env
    restart: unless-stopped

volumes:
  backend_storage:
